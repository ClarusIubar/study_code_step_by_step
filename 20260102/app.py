
# main실행 테스트를 위한 임시골격
class LottoApp:
    def __init__(self, root):
        self.root = root
        self.root.title("모든 것이 랜덤인 로또")

    # 동작 버튼(사용자 동작)
    def user_press_button():
        return

    # 볼 객체 생성(그리기)
    def generate_ball():
        return

    # 볼 움직임 제어(움직임)
    def controll_ball():
        return

    # 팝업 생성(결과 표출)
    def pop_up():
        return